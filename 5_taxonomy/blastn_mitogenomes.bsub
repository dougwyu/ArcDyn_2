######## bsub ############
#!/bin/sh
#BSUB -q long-eth     # on hpc.uea.ac.uk, mellanox-ib (168 hours = 7 days)
#BSUB -J mitogblast
#BSUB -oo mitogblast.out
#BSUB -eo mitogblast.err
#BSUB -R "rusage[mem=63000]"  # max mem for mellanox-ib is 128GB = 128000
#BSUB -M 63000
#BSUB -x # exclusive because i want a bunch of cores
#BSUB -B        # sends email to me when job starts
#BSUB -N        # sends email to me when job finishes

. /etc/profile
module load ncbi-blast/2.6.0+/gcc # 2.6.0 is the version that can use -remote because uses https
blastn -db nt -query ~/greenland_2016/2016MayJuneDec_and_8mix2015_greenland_13mtgenes_ref.fasta -task blastn -outfmt 5 -out 2016MayJuneDec_and_8mix2015_greenland_13mtgenes_ref.xml -max_target_seqs 100 -remote

# -max_target_seqs 100 # if i want to reduce the size of the output file
# -outfmt 5 is for xml for megan analysis
# -num_threads 16 is incompatible with -remote;  it's only for running locally
# mellanox-ib has 28 cores and 128 GB RAM
######## bsub ############
